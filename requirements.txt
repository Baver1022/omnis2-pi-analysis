# OMNIS2 - Analiza Statystyczna 10 Miliardów Cyfr Liczby Pi
# Requirements dla analizy statystycznej i generowania raportów

# Core scientific computing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<2.0.0
pandas>=2.0.0,<3.0.0

# GPU acceleration (opcjonalne - dla NVIDIA RTX)
# Zainstaluj odpowiednią wersję w zależności od CUDA:
# cupy-cuda12x>=12.0.0  # dla CUDA 12.x
# cupy-cuda11x>=11.0.0  # dla CUDA 11.x

# Data visualization
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# LaTeX generation (opcjonalne - jeśli używasz bezpośrednio)
# texlive-latex-base
# texlive-latex-extra
# texlive-fonts-recommended

# Machine Learning (dla testu LSTM)
tensorflow>=2.13.0,<3.0.0  # opcjonalne

# Progress bars
tqdm>=4.65.0,<5.0.0

# JSON handling (built-in, ale dla kompletności)
# json - built-in

# Path handling (built-in, ale dla kompletności)
# pathlib - built-in

